# ─────────────────────────────────────────────────────────────────────────────
# SGEIA — Smart Grid Energy Intelligence Assistant
# requirements.txt
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core LLM & LangChain ──────────────────────────────────────────────────────
openai>=1.30.1
langchain>=0.2.5
langchain-openai>=0.1.8
langchain-community>=0.2.5
langchain-core>=0.2.22
langgraph>=0.1.19

# ── Fallback LLM providers (free tiers) ───────────────────────────────────────
langchain-google-genai>=1.0.6       # Gemini 1.5 Pro / Flash fallback
groq>=0.9.0                         # Groq Llama fallback

# ── Vector Database ───────────────────────────────────────────────────────────
chromadb>=0.5.3

# ── Embeddings & Reranking ────────────────────────────────────────────────────
sentence-transformers>=3.0.1        # Cross-Encoder reranker + local embedding fallback

# ── Hybrid Search ────────────────────────────────────────────────────────────
rank-bm25==0.2.2

# ── ML Models ────────────────────────────────────────────────────────────────
xgboost>=2.0.3
scikit-learn>=1.5.0
shap>=0.45.1
joblib>=1.4.2

# ── Forecasting ───────────────────────────────────────────────────────────────
prophet>=1.1.5

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.2.2
numpy>=1.26.4,<2.0.0

# ── Guardrails & PII ─────────────────────────────────────────────────────────
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.5

# ── Evaluation ────────────────────────────────────────────────────────────────
deepeval>=0.21.73

# ── API Backend ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9
sse-starlette>=2.1.0                # Server-Sent Events for streaming

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0
altair>=5.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.7.4
pydantic-settings>=2.3.1
httpx>=0.27.0
tenacity>=8.3.0                     # Retry logic for LLM fallbacks
tqdm>=4.66.4
colorlog>=6.8.2                     # Coloured console logging
